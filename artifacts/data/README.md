# Raw Data Not Included

The full raw dataset was not copied into this GitHub-ready folder.

Expected local location relative to the repo root on this machine:

```text
../data
```

Why it is excluded:

- it is approximately 168 GB
- it does not belong in a normal GitHub repository
- the training code can point to it externally with `--data-root` or `EMULATOR_DATA_ROOT`

Expected structure is documented in [../../docs/data_layout.md](../../docs/data_layout.md).
