# Stats

`stats_full.pt` contains the mean and standard deviation tensors used to normalize the full dataset.

It was copied from the original workflow because it is lightweight and directly required by training and inference.
